# Archive

Retired plugins, kept for reference and parts recycling. **Not** listed in
`.claude-plugin/marketplace.json`, so they are not offered or installable via the marketplace.

| Plugin | Was |
|--------|-----|
| `aipilot` | Multi-agent pipeline (Opus plans/implements, Codex reviews each step as a quality gate). |
| `second-opinion` | `/second-opinion` — independent analysis via Codex CLI (Opus fallback) when stuck. |
| `chunkhound-integration` | Semantic code research with ChunkHound (multi-hop search, LLM synthesis, routing). |

To revive one: move it back under `plugins/` and re-add its entry to `marketplace.json`.
